diff --git a/dist/index.js b/dist/index.js
index dca128d3a790378c51a24a16d92585178343b278..851335c82080752677c642e48530277cbd1127b8 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -181,21 +181,6 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                if (part.mediaType === "application/pdf") {
-                  if (part.data instanceof URL) {
-                    throw new import_provider.UnsupportedFunctionalityError({
-                      functionality: "PDF file parts with URLs"
-                    });
-                  }
-                  return {
-                    type: "file",
-                    file: {
-                      filename: (_a2 = part.filename) != null ? _a2 : "document.pdf",
-                      file_data: `data:application/pdf;base64,${(0, import_provider_utils.convertToBase64)(part.data)}`
-                    },
-                    ...partMetadata
-                  };
-                }
                 if (part.mediaType.startsWith("text/")) {
                   const textContent = part.data instanceof URL ? part.data.toString() : typeof part.data === "string" ? new TextDecoder().decode(
                     (0, import_provider_utils.convertBase64ToUint8Array)(part.data)
@@ -206,9 +191,19 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                throw new import_provider.UnsupportedFunctionalityError({
-                  functionality: `file part media type ${part.mediaType}`
-                });
+                if (part.data instanceof URL) {
+                  throw new import_provider.UnsupportedFunctionalityError({
+                    functionality: `file parts with URLs for media type ${part.mediaType}`
+                  });
+                }
+                return {
+                  type: "file",
+                  file: {
+                    filename: (_a2 = part.filename) != null ? _a2 : part.mediaType === "application/pdf" ? "document.pdf" : "document",
+                    file_data: `data:${part.mediaType};base64,${(0, import_provider_utils.convertToBase64)(part.data)}`
+                  },
+                  ...partMetadata
+                };
               }
             }
           }),
@@ -696,7 +691,7 @@ var OpenAICompatibleChatLanguageModel = class {
               finishReason = { unified: "error", raw: void 0 };
               controller.enqueue({
                 type: "error",
-                error: chunk.value.error.message
+                error: chunk.value.error
               });
               return;
             }
diff --git a/dist/index.mjs b/dist/index.mjs
index 3b1e1b6bdec5032e3b4fa5ffbcc8cdf3dfe1cc40..f19ceb3e71200ebd277253378d09e8cf6799f413 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -166,21 +166,6 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                if (part.mediaType === "application/pdf") {
-                  if (part.data instanceof URL) {
-                    throw new UnsupportedFunctionalityError({
-                      functionality: "PDF file parts with URLs"
-                    });
-                  }
-                  return {
-                    type: "file",
-                    file: {
-                      filename: (_a2 = part.filename) != null ? _a2 : "document.pdf",
-                      file_data: `data:application/pdf;base64,${convertToBase64(part.data)}`
-                    },
-                    ...partMetadata
-                  };
-                }
                 if (part.mediaType.startsWith("text/")) {
                   const textContent = part.data instanceof URL ? part.data.toString() : typeof part.data === "string" ? new TextDecoder().decode(
                     convertBase64ToUint8Array(part.data)
@@ -191,9 +176,19 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                throw new UnsupportedFunctionalityError({
-                  functionality: `file part media type ${part.mediaType}`
-                });
+                if (part.data instanceof URL) {
+                  throw new UnsupportedFunctionalityError({
+                    functionality: `file parts with URLs for media type ${part.mediaType}`
+                  });
+                }
+                return {
+                  type: "file",
+                  file: {
+                    filename: (_a2 = part.filename) != null ? _a2 : part.mediaType === "application/pdf" ? "document.pdf" : "document",
+                    file_data: `data:${part.mediaType};base64,${convertToBase64(part.data)}`
+                  },
+                  ...partMetadata
+                };
               }
             }
           }),
@@ -683,7 +678,7 @@ var OpenAICompatibleChatLanguageModel = class {
               finishReason = { unified: "error", raw: void 0 };
               controller.enqueue({
                 type: "error",
-                error: chunk.value.error.message
+                error: chunk.value.error
               });
               return;
             }
diff --git a/dist/internal/index.js b/dist/internal/index.js
index 0858632db778d30b303fbc9d5d46a29582d6e5b5..b870c60310e83c0f49f8d5915ebeadd5326eee57 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -106,21 +106,6 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                if (part.mediaType === "application/pdf") {
-                  if (part.data instanceof URL) {
-                    throw new import_provider.UnsupportedFunctionalityError({
-                      functionality: "PDF file parts with URLs"
-                    });
-                  }
-                  return {
-                    type: "file",
-                    file: {
-                      filename: (_a2 = part.filename) != null ? _a2 : "document.pdf",
-                      file_data: `data:application/pdf;base64,${(0, import_provider_utils.convertToBase64)(part.data)}`
-                    },
-                    ...partMetadata
-                  };
-                }
                 if (part.mediaType.startsWith("text/")) {
                   const textContent = part.data instanceof URL ? part.data.toString() : typeof part.data === "string" ? new TextDecoder().decode(
                     (0, import_provider_utils.convertBase64ToUint8Array)(part.data)
@@ -131,9 +116,19 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                throw new import_provider.UnsupportedFunctionalityError({
-                  functionality: `file part media type ${part.mediaType}`
-                });
+                if (part.data instanceof URL) {
+                  throw new import_provider.UnsupportedFunctionalityError({
+                    functionality: `file parts with URLs for media type ${part.mediaType}`
+                  });
+                }
+                return {
+                  type: "file",
+                  file: {
+                    filename: (_a2 = part.filename) != null ? _a2 : part.mediaType === "application/pdf" ? "document.pdf" : "document",
+                    file_data: `data:${part.mediaType};base64,${(0, import_provider_utils.convertToBase64)(part.data)}`
+                  },
+                  ...partMetadata
+                };
               }
             }
           }),
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 116b2deee86bf07cf981a64c000c7a4f3e42b52e..d41c783334ab6825fce15fd8e2fe0dd5c8216506 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -81,21 +81,6 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                if (part.mediaType === "application/pdf") {
-                  if (part.data instanceof URL) {
-                    throw new UnsupportedFunctionalityError({
-                      functionality: "PDF file parts with URLs"
-                    });
-                  }
-                  return {
-                    type: "file",
-                    file: {
-                      filename: (_a2 = part.filename) != null ? _a2 : "document.pdf",
-                      file_data: `data:application/pdf;base64,${convertToBase64(part.data)}`
-                    },
-                    ...partMetadata
-                  };
-                }
                 if (part.mediaType.startsWith("text/")) {
                   const textContent = part.data instanceof URL ? part.data.toString() : typeof part.data === "string" ? new TextDecoder().decode(
                     convertBase64ToUint8Array(part.data)
@@ -106,9 +91,19 @@ function convertToOpenAICompatibleChatMessages(prompt) {
                     ...partMetadata
                   };
                 }
-                throw new UnsupportedFunctionalityError({
-                  functionality: `file part media type ${part.mediaType}`
-                });
+                if (part.data instanceof URL) {
+                  throw new UnsupportedFunctionalityError({
+                    functionality: `file parts with URLs for media type ${part.mediaType}`
+                  });
+                }
+                return {
+                  type: "file",
+                  file: {
+                    filename: (_a2 = part.filename) != null ? _a2 : part.mediaType === "application/pdf" ? "document.pdf" : "document",
+                    file_data: `data:${part.mediaType};base64,${convertToBase64(part.data)}`
+                  },
+                  ...partMetadata
+                };
               }
             }
           }),
diff --git a/src/chat/convert-to-openai-compatible-chat-messages.ts b/src/chat/convert-to-openai-compatible-chat-messages.ts
index dd8b7b7659c21de07f875cc23188ac7f7c8290a8..8490f3cafb99c1fa7eff4de73c01e6c34afda3a1 100644
--- a/src/chat/convert-to-openai-compatible-chat-messages.ts
+++ b/src/chat/convert-to-openai-compatible-chat-messages.ts
@@ -103,17 +102,0 @@ export function convertToOpenAICompatibleChatMessages(
-                if (part.mediaType === 'application/pdf') {
-                  if (part.data instanceof URL) {
-                    throw new UnsupportedFunctionalityError({
-                      functionality: 'PDF file parts with URLs',
-                    });
-                  }
-
-                  return {
-                    type: 'file',
-                    file: {
-                      filename: part.filename ?? 'document.pdf',
-                      file_data: `data:application/pdf;base64,${convertToBase64(part.data)}`,
-                    },
-                    ...partMetadata,
-                  };
-                }
-
@@ -137,4 +120,18 @@ export function convertToOpenAICompatibleChatMessages(
-                // Unsupported type
-                throw new UnsupportedFunctionalityError({
-                  functionality: `file part media type ${part.mediaType}`,
-                });
+                if (part.data instanceof URL) {
+                  throw new UnsupportedFunctionalityError({
+                    functionality: `file parts with URLs for media type ${part.mediaType}`,
+                  });
+                }
+
+                return {
+                  type: 'file',
+                  file: {
+                    filename:
+                      part.filename ??
+                      (part.mediaType === 'application/pdf'
+                        ? 'document.pdf'
+                        : 'document'),
+                    file_data: `data:${part.mediaType};base64,${convertToBase64(part.data)}`,
+                  },
+                  ...partMetadata,
+                };
diff --git a/src/chat/openai-compatible-chat-language-model.ts b/src/chat/openai-compatible-chat-language-model.ts
index 8c622db23c2d9a7373701f5a1b0c2ba109e24602..643c3db68a6043e097edc1122e0eb53fd13495c5 100644
--- a/src/chat/openai-compatible-chat-language-model.ts
+++ b/src/chat/openai-compatible-chat-language-model.ts
@@ -442,7 +442,7 @@ export class OpenAICompatibleChatLanguageModel implements LanguageModelV3 {
               finishReason = { unified: 'error', raw: undefined };
               controller.enqueue({
                 type: 'error',
-                error: chunk.value.error.message,
+                error: chunk.value.error,
               });
               return;
             }
